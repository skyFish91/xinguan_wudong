# 乌东文旅平台 - 团队部署指南

## 📋 快速开始

每个团队成员拉取代码后，按照以下步骤在本地环境部署项目。

---

## 🔧 前置要求

1. **Node.js**：版本 **>= 18**（建议 20/22 LTS）。Vite 5 在 Node 16 上跑不起来
2. **MySQL**：版本 >= 5.7 或 8.0
3. **Redis**：版本 >= 5.0（可选，用于缓存）
4. **Git**：用于拉取代码

---

## 📥 步骤 1：克隆项目

```bash
git clone https://github.com/skyFish91/xinguan_wudong.git
cd xinguan_wudong
```

---

## 🗄️ 步骤 2：初始化数据库

### 2.1 创建数据库

在 MySQL 中创建数据库：

```sql
CREATE DATABASE wudong CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2.2 导入数据库脚本

按顺序执行项目中的 SQL 脚本：

```bash
# 方式 1：使用 MySQL 命令行
mysql -u root -p wudong < sql/01-ddl.sql
mysql -u root -p wudong < sql/02-dml.sql
mysql -u root -p wudong < sql/03-community-seed.sql

# 方式 2：使用 MySQL Workbench 或其他 GUI 工具
# 依次打开 sql/01-ddl.sql、02-dml.sql、03-community-seed.sql 执行
```

**说明**（**必须按 1→2→3 顺序执行**）：
- `01-ddl.sql`：创建数据表结构
- `02-dml.sql`：插入初始数据（账号、商品、订单等）
- `03-community-seed.sql`：社区演示数据（6 篇游记 + 7 条评论 + 5 个话题），
  不导入的话社区分享页是空的

---

## ⚙️ 步骤 3：配置后端环境变量

### 3.1 复制环境变量模板

```bash
cd server
cp .env.example .env
```

### 3.2 修改 `.env` 文件

打开 `server/.env`，根据你的本地环境修改以下配置：

```env
# 数据库配置（必须修改）
DB_HOST=127.0.0.1
DB_PORT=3306                    # 你的 MySQL 端口
DB_USER=root                    # 你的 MySQL 用户名
DB_PASSWORD=your_password       # 你的 MySQL 密码
DB_NAME=wudong

# Redis 配置（如果你有 Redis）
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# 其他配置保持默认即可
SERVER_PORT=7001
APP_KEYS=wudong-secret-key-2026
JWT_SECRET=wudong-jwt-secret-2026
```

**重要**：`.env` 文件包含敏感信息，已加入 `.gitignore`，不会提交到 Git。

---

## 🚀 步骤 4：启动后端

```bash
# 确保在 server 目录下
cd server

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

**成功标志**：
```
✅ 乌东文旅平台后端启动完成
🌐 HTTP 服务器运行在: http://127.0.0.1:7001
```

**测试后端是否正常**：
```bash
curl http://localhost:7001/api/home
```

应该返回 JSON 数据。

---

## 🎨 步骤 5：启动前端（C 端）

> ⚠️ **注意目录名！** C 端代码在 **`qianduan0/web`**，不是根目录的 `web/`。
> 根目录 `web/` 是**已废弃的半迁移版本**（请求基址为空、代理指向未部署的 8001、路由会解析到旧文件），
> 直接 `cd web` 启动会看到页面但所有接口都调不通。

打开新的终端窗口：

```bash
# 进入 C 端目录（注意是 qianduan0/web）
cd qianduan0/web

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

**成功标志**：
```
  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

在浏览器访问 `http://localhost:5173`，应该能看到首页。

---

## 🛠️ 步骤 6：启动管理后台（可选）

再开一个终端窗口：

```bash
cd admin
npm install
npm run dev          # → http://localhost:5174
```

后台分两套视图，用不同角色的账号登录会看到不同内容：
- **管理员**（`13800000000`）看到用户/订单/内容审核/运营/财务等 8 个页面
- **商家**（`13800000002`~`05`）只看到自己模块（衣/食/住/行）的经营页面

---

## ✅ 步骤 7：验证功能

### 测试基础功能

1. **首页**：`http://localhost:5173/`
2. **服饰列表**：`http://localhost:5173/clothing`
3. **美食列表**：`http://localhost:5173/food`
4. **住宿列表**：`http://localhost:5173/hotel`
5. **景点列表**：`http://localhost:5173/travel`

### 测试用户功能

1. **注册账号**：`http://localhost:5173/register`
2. **登录**：`http://localhost:5173/login`
3. **购物车**：`http://localhost:5173/cart`（需登录）
4. **订单**：`http://localhost:5173/orders`（需登录）

### 测试管理后台

1. **后台登录**：`http://localhost:5174/`

| 角色 | 账号 | 密码 | 说明 |
|---|---|---|---|
| 平台管理员 | `13800000000` | `admin123` | 全部后台功能 |
| 演示用户 | `13800000001` | `user123` | C 端用户 |
| 商家-服饰 | `13800000002` | `merchant123` | 衣 |
| 商家-美食 | `13800000003` | `merchant123` | 食 |
| 商家-住宿 | `13800000004` | `merchant123` | 住 |
| 商家-出行 | `13800000005` | `merchant123` | 行 |

C 端短信验证码在开发模式下固定为 **`123456`**，不会真发短信。

---

## 🐛 常见问题

### Q1: 后端启动报错 "Access denied for user"

**原因**：数据库密码配置错误

**解决**：
1. 检查 `server/.env` 中的 `DB_PASSWORD` 是否正确
2. 确认 MySQL 用户名和密码能正常登录

### Q2: 后端启动报错 "Unknown database 'wudong'"

**原因**：数据库未创建

**解决**：
```sql
CREATE DATABASE wudong CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### Q3: 前端页面显示 "网络错误" 或 500

**原因**：后端未启动或端口不对

**解决**：
1. 确认后端已启动：`curl http://localhost:7001/api/home`
2. 检查前端代理配置：`qianduan0/web/vite.config.ts` 中的 `proxy` 是否指向 `http://127.0.0.1:7001`
3. 确认你启动的是 `qianduan0/web` 而不是根目录的 `web/`

> 💡 本机若开了代理软件（如 Clash），curl 测本地服务建议加 `--noproxy '*'`，
> 否则请求可能被代理截胡，报出与代码无关的莫须有错误。

### Q4: npm install 很慢

**解决**：使用国内镜像
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

### Q5: Redis 连接失败

**说明**：Redis 是可选的，如果本地没安装 Redis，后端会跳过 Redis 功能，不影响基本使用。

**如果需要 Redis**：
- Windows：下载并安装 Redis for Windows
- Mac/Linux：`brew install redis` 或 `apt install redis`

---

## 📁 项目结构

```
xinguan_wudong/
├── server/              # ✅ 后端（Midway.js v4 + TypeORM），端口 7001
│   ├── src/
│   │   ├── module/     # 业务模块
│   │   │   ├── travel/ # 出行模块
│   │   │   ├── food/   # 美食模块
│   │   │   ├── hotel/  # 住宿模块
│   │   │   ├── clothing/ # 服饰模块
│   │   │   ├── community/ # 社区模块
│   │   │   └── ai/     # AI 聊天模块
│   │   └── entity/     # TypeORM 实体
│   ├── .env            # 环境变量（需自己创建，勿提交）
│   └── package.json
├── qianduan0/web/       # ✅ C 端（Vue 3 + Vite），端口 5173 —— 唯一在用的前端
│   └── src/
├── admin/              # ✅ 管理后台（Vue 3），端口 5174
├── sql/                # 数据库脚本
│   ├── 01-ddl.sql     # 表结构
│   ├── 02-dml.sql     # 初始数据
│   └── 03-community-seed.sql  # 社区演示数据（6 游记 + 7 评论 + 5 话题）
└── README.md
```

### ⚠️ 这些目录是历史遗留，不要基于它们开发

仓库里还有十几个顶层目录（`web/`、`wudong-server/`、`wudong-web/`、`web-pc/`、
`cool-admin-midway/`、`cool-admin-vue/`、`food-module/`、`住模块/` 等），
都是早期独立版本或半迁移产物，内容已并入上面的三个在役目录。

**判断方法**：看 `package.json` 的 `dev` 脚本端口，以及 `vite.config` 的代理 `target`
是否指向 `7001`。指向 8001 或缺代理的一律是废弃版。

---

## 🔒 Git 协作规范

### .gitignore 已配置忽略

- `server/.env`（环境变量，包含敏感信息）
- `node_modules/`（依赖包）
- `dist/`（构建产物）

### 提交代码前

1. **不要提交 `.env` 文件**
2. **不要提交 `node_modules/`**
3. 如果修改了数据库结构，记得更新 `sql/01-ddl.sql`

### 拉取代码后

1. 执行 `npm install`
2. 复制 `.env.example` 为 `.env` 并配置
3. 导入最新的数据库脚本

---

## 📞 技术支持

如遇到问题，请：
1. 查看本文档的"常见问题"章节
2. 查看项目 GitHub Issues
3. 联系团队成员

---

## 📝 开发建议

1. **数据库变更**：任何表结构修改都要同步更新 `sql/01-ddl.sql`
2. **API 文档**：使用 Postman 或 Swagger 记录 API 接口
3. **分支管理**：功能开发建议创建独立分支，完成后合并到 main
4. **代码规范**：提交前执行 `npm run lint` 检查代码风格

---

**最后更新**：2026-09-10  
**文档版本**：v1.0
