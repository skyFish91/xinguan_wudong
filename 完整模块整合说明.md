# 乌东文旅平台 - 完整模块整合说明文档

## 📋 概述

本次整合包含两个主要模块：
1. **出行模块（旅游模块）** - 景区、路线、电子票、交通指南
2. **AI 聊天模块** - 智能客服助手

所有代码已完成整合并推送到 GitHub，需要后端同学协助配置数据库权限以完成最后的部署。

---

## ✅ 已完成的工作

### 一、出行模块整合

#### 后端整合（`server/src/module/travel/`）

**控制器 (Controller)**:
- `scenic.controller.ts` - 景区管理接口
- `route.controller.ts` - 旅游路线接口
- `ticket.controller.ts` - 电子票务接口
- `traffic-guide.controller.ts` - 交通指南接口

**服务层 (Service)**:
- `scenic.service.ts` - 景区业务逻辑
- `route.service.ts` - 路线业务逻辑
- `ticket.service.ts` - 票务业务逻辑
- `traffic-guide.service.ts` - 交通指南业务逻辑

**数据实体 (Entity)**:
- `scenic.entity.ts` - 景区数据表
- `route.entity.ts` - 路线数据表
- `route-day.entity.ts` - 路线行程数据表
- `e-ticket.entity.ts` - 电子票数据表
- `ticket-type.entity.ts` - 票种数据表
- `ticket-stock.entity.ts` - 票务库存表
- `review.entity.ts` - 评价数据表
- `traffic-guide.entity.ts` - 交通指南表

**API 接口**:
- `GET /api/travel/scenics` - 获取景区列表
- `GET /api/travel/scenics/:id` - 获取景区详情
- `POST /api/travel/scenics/:id/reviews` - 提交景区评价
- `GET /api/travel/routes` - 获取路线列表
- `GET /api/travel/routes/:id` - 获取路线详情
- `POST /api/travel/tickets/book` - 预订电子票
- `GET /api/travel/tickets/my-tickets` - 获取我的电子票
- `GET /api/travel/traffic-guides` - 获取交通指南

#### 前端整合（`web/src/views/travel/`）

**页面组件**:
- `Scenics.vue` - 景区首页
- `ScenicList.vue` - 景区列表页
- `ScenicDetail.vue` - 景区详情页（含预订、评价、图片画廊）
- `Routes.vue` - 路线首页
- `RouteList.vue` - 路线列表页
- `RouteDetail.vue` - 路线详情页（含行程详情、预订）
- `Search.vue` - 搜索页面（景区、路线搜索）
- `Guides.vue` - 交通指南页面
- `MyEtickets.vue` - 我的电子票页面

**功能特性**:
- ✅ 景区浏览、搜索、筛选
- ✅ 路线浏览、详情查看
- ✅ 电子票在线预订
- ✅ 景区评价与评分
- ✅ 收藏功能
- ✅ 图片画廊展示
- ✅ 响应式设计

#### 路由配置（`web/src/router/index.ts`）

```javascript
// 出行模块路由
{ path: '/travel', component: () => import('../views/travel/Scenics.vue') },
{ path: '/travel/scenics', component: () => import('../views/travel/ScenicList.vue') },
{ path: '/travel/scenics/:id', component: () => import('../views/travel/ScenicDetail.vue') },
{ path: '/travel/routes', component: () => import('../views/travel/Routes.vue') },
{ path: '/travel/routes-list', component: () => import('../views/travel/RouteList.vue') },
{ path: '/travel/routes/:id', component: () => import('../views/travel/RouteDetail.vue') },
{ path: '/travel/search', component: () => import('../views/travel/Search.vue') },
{ path: '/travel/guides', component: () => import('../views/travel/Guides.vue') },
{ path: '/travel/my-etickets', component: () => import('../views/travel/MyEtickets.vue') },
```

---

### 二、AI 聊天模块整合

#### 后端整合（`server/src/module/ai/`）

**控制器**:
- `ai.controller.ts` - AI 聊天接口

**服务层**:
- `ai.service.ts` - 通义千问 API 调用
- `conversation.service.ts` - 对话历史管理

**数据实体**:
- `ai_conversation.entity.ts` - 对话表
- `ai_message.entity.ts` - 消息表

**API 接口**:
- `POST /api/ai/chat` - AI 聊天接口
- `GET /api/ai/conversations` - 获取对话列表
- `GET /api/ai/conversations/:id/messages` - 获取对话历史
- `DELETE /api/ai/conversations/:id` - 删除对话

#### 前端整合（`web/src/views/ai/`）

**页面组件**:
- `Chat.vue` - AI 聊天页面（完整的聊天 UI）

**API 封装**:
- `web/src/api/ai.ts` - AI 接口封装

**功能特性**:
- ✅ 实时对话
- ✅ 对话历史保存
- ✅ 打字动画效果
- ✅ 快捷问题建议
- ✅ 美观的聊天界面

#### 路由配置

```javascript
// AI 模块路由
{ path: '/ai', component: () => import('../views/ai/Chat.vue') },
{ path: '/ai/chat', component: () => import('../views/ai/Chat.vue') },
```

#### 导航栏入口

已在 `web/src/components/TopNav.vue` 添加 "🤖 AI助手" 菜单项。

---

## ❌ 需要解决的问题

### MySQL 数据库权限配置

**问题现象**:
```
Error: Access denied for user 'root'@'172.18.0.1' (using password: YES)
```

**原因**: 
- 后端从宿主机（Windows）通过 Docker 网络连接 MySQL 容器
- MySQL 的 root 用户没有正确授权 Docker 网络地址访问

---

## 🔧 后端同学需要做的事情

### 方法 1：授权现有用户（推荐）

```bash
# 进入 MySQL 容器
docker exec -it mysql-wudong mysql -u root -p15715659594

# 在 MySQL 命令行中执行
GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' WITH GRANT OPTION;
FLUSH PRIVILEGES;
EXIT;
```

### 方法 2：修改 MySQL 容器配置

如果方法 1 不行，可以重启 MySQL 容器并添加启动参数：

```bash
# 停止容器
docker stop mysql-wudong

# 启动容器（添加 --skip-grant-tables 暂时跳过权限检查）
docker start mysql-wudong

# 进入容器修复权限
docker exec -it mysql-wudong mysql -u root -p15715659594

# 在 MySQL 中执行
FLUSH PRIVILEGES;
ALTER USER 'root'@'%' IDENTIFIED BY '15715659594';
GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' WITH GRANT OPTION;
FLUSH PRIVILEGES;
EXIT;

# 重启容器应用更改
docker restart mysql-wudong
```

### 方法 3：重建 MySQL 容器（最彻底，但需要重新导入数据）

```bash
# 备份数据
docker exec mysql-wudong mysqldump -u root -p15715659594 --all-databases > backup.sql

# 停止并删除旧容器
docker stop mysql-wudong
docker rm mysql-wudong

# 创建新容器
docker run -d \
  --name mysql-wudong \
  -p 3307:3306 \
  -e MYSQL_ROOT_PASSWORD=15715659594 \
  -e MYSQL_DATABASE=wudong \
  mysql:8.0 \
  --default-authentication-plugin=mysql_native_password

# 等待 MySQL 启动
sleep 10

# 恢复数据
docker exec -i mysql-wudong mysql -u root -p15715659594 < backup.sql
```

---

## 🚀 启动步骤

### 1. 确认环境

```bash
# 检查 MySQL 是否运行
docker ps | grep mysql-wudong

# 检查 Redis 是否运行
docker ps | grep redis
```

### 2. 配置数据库权限

执行上面"方法 1"中的命令。

### 3. 启动后端

```bash
cd server
npm install
npm run dev
```

**成功标志**: 
- 看到 `乌东文旅平台后端启动完成`
- 端口 `7001` 监听成功
- 没有 MySQL 权限错误

### 4. 启动前端

```bash
cd web
npm install
npm run dev
```

**成功标志**: 
- 看到 `Local: http://localhost:5173`
- 可以在浏览器访问

### 5. 测试功能

#### 测试出行模块

**浏览器访问**:
- 首页: `http://localhost:5173`
- 景区列表: `http://localhost:5173/travel/scenics`
- 路线列表: `http://localhost:5173/travel/routes`

**用 curl 测试后端 API**:
```bash
# 获取景区列表
curl http://localhost:7001/api/travel/scenics

# 获取路线列表
curl http://localhost:7001/api/travel/routes
```

#### 测试 AI 模块

**浏览器访问**:
- AI 聊天: `http://localhost:5173/ai`

**用 curl 测试**:
```bash
curl -X POST http://localhost:7001/api/ai/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"乌东有哪些著名景点？","conversationId":"test123"}'
```

**预期响应**:
```json
{
  "success": true,
  "data": {
    "message": "AI 的回复内容...",
    "conversationId": "test123",
    "messageId": "消息ID"
  }
}
```

---

## 📦 数据库表结构

### 出行模块数据表

#### `scenic` - 景区表
```sql
- id: 主键
- name: 景区名称
- description: 景区描述
- main_image: 主图
- address: 地址
- ticket_price: 门票价格
- rating: 评分
- status: 状态（0-关闭，1-开放）
- created_at, updated_at
```

#### `route` - 路线表
```sql
- id: 主键
- title: 路线标题
- description: 路线描述
- cover_image: 封面图
- days: 天数
- price: 价格
- difficulty: 难度
- created_at, updated_at
```

#### `route_day` - 路线行程表
```sql
- id: 主键
- route_id: 路线ID（外键）
- day_number: 第几天
- title: 标题
- description: 描述
- activities: 活动安排
```

#### `e_ticket` - 电子票表
```sql
- id: 主键
- user_id: 用户ID
- scenic_id: 景区ID
- ticket_code: 票号
- visit_date: 游玩日期
- quantity: 数量
- total_price: 总价
- status: 状态（0-未使用，1-已使用，2-已退票）
- created_at, updated_at
```

#### `review` - 评价表
```sql
- id: 主键
- user_id: 用户ID
- scenic_id: 景区ID
- rating: 评分（1-5）
- content: 评价内容
- created_at, updated_at
```

#### `traffic_guide` - 交通指南表
```sql
- id: 主键
- scenic_id: 景区ID
- transport_type: 交通方式（bus/car/train）
- description: 说明
- estimated_time: 预计时间
- estimated_cost: 预计费用
```

### AI 模块数据表

#### `ai_conversation` - 对话表
```sql
- id: 主键
- conversation_id: 对话唯一标识
- user_id: 用户ID（可选）
- title: 对话标题
- created_at, updated_at
```

#### `ai_message` - 消息表
```sql
- id: 主键
- conversation_id: 关联对话ID
- role: 角色（user/assistant）
- content: 消息内容
- created_at
```

---

## 🔧 配置文件

### `server/.env`
```env
# 后端环境变量
APP_KEYS=wudong-secret-key-2026
SERVER_PORT=7001
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=15715659594
DB_NAME=wudong
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
JWT_SECRET=wudong-jwt-secret-2026
QIANWEN_API_KEY=你的通义千问API密钥
```

### `web/vite.config.ts`
已配置路径别名和 API 代理：
```typescript
resolve: {
  alias: {
    '@': path.resolve(__dirname, './src'),
  },
},
server: {
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://127.0.0.1:7001',
      changeOrigin: true,
    },
  },
}
```

---

## 📝 GitHub 提交记录

**仓库**: `skyFish91/xinguan_wudong`  
**分支**: `main`

**最近提交**:
1. `4a3fd80` - feat: 前端AI聊天功能整合
2. `7ace216` - feat: 整合旅游模块到统一后端

---

## 🆘 常见问题

### Q1: 后端启动后前端还是 500 错误？
**A**: 
1. 检查后端是否真的在运行：`curl http://localhost:7001/api/home`
2. 检查浏览器控制台的具体错误信息
3. 查看后端日志是否有报错

### Q2: 前端页面空白或数据不显示？
**A**: 
1. 确认后端已启动
2. 打开浏览器控制台（F12），查看 Network 标签页
3. 检查 API 请求是否成功返回数据

### Q3: AI 接口返回错误？
**A**: 
1. 检查 `.env` 文件中的 `QIANWEN_API_KEY` 是否配置
2. 检查网络是否能访问通义千问 API
3. 查看后端日志获取详细错误信息

### Q4: 如何查看后端日志？
**A**: 
```bash
# 如果后端在前台运行，直接查看终端输出
# 如果后端在后台运行，查看日志文件
cd server
tail -f logs/midway-web.log
```

### Q5: 数据库连接失败？
**A**: 
1. 确认 MySQL 容器正在运行: `docker ps | grep mysql`
2. 确认密码正确: `15715659594`
3. 执行本文档"方法 1"中的权限配置命令

---

## 📞 联系方式

如有问题，请联系：
- **出行模块负责人**: （你的联系方式）
- **AI 模块负责人**: （你的联系方式）
- **GitHub**: skyFish91/xinguan_wudong

---

## ✅ 验收标准

### 后端验收
- [ ] 后端启动无错误
- [ ] 景区列表 API 返回数据
- [ ] 路线列表 API 返回数据
- [ ] AI 聊天 API 返回响应
- [ ] 电子票预订功能正常

### 前端验收
- [ ] 首页正常显示
- [ ] 景区列表页显示景区数据
- [ ] 景区详情页功能完整（图片、评价、预订）
- [ ] 路线列表页显示路线数据
- [ ] 路线详情页功能完整
- [ ] 搜索功能正常
- [ ] AI 聊天页面可正常对话
- [ ] 导航栏所有菜单可点击

---

**最后更新**: 2026-09-10  
**文档版本**: v1.0
