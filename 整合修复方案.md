# 乌东文旅平台 - 团队整合修复方案

## 📋 一、项目架构确认

### 1.1 最终确定的主体结构

```
xinguan_wudong/
├── server/              # ✅ 主后端（Midway.js，端口7001）
│   ├── src/
│   │   ├── module/     # 所有业务模块
│   │   │   ├── clothing/   # 衣模块
│   │   │   ├── food/       # 食模块  
│   │   │   ├── hotel/      # 住模块
│   │   │   ├── travel/     # 行模块
│   │   │   ├── community/  # 社区模块
│   │   │   ├── ai/         # AI模块
│   │   │   ├── admin/      # 管理后台接口
│   │   │   ├── auth/       # 认证
│   │   │   ├── user/       # 用户
│   │   │   ├── order/      # 订单
│   │   │   ├── pay/        # 支付
│   │   │   └── ...
│   │   ├── entity/     # 数据实体
│   │   └── common/     # 公共模块
│   ├── .env            # 环境配置
│   └── package.json
│
├── admin/               # ✅ 管理后台（Vue3，端口5174）
│   ├── src/
│   │   ├── views/
│   │   ├── router/
│   │   └── api/
│   └── package.json
│
├── web/                 # ✅ PC用户端（Vue3，端口5173）
│   ├── src/
│   │   ├── views/
│   │   ├── router/
│   │   └── api/
│   └── package.json
│
└── sql/                 # ✅ 数据库脚本
    ├── 01-ddl.sql       # 数据库结构
    └── 02-dml.sql       # 初始数据
```

### 1.2 需要废弃的目录（不使用）

```
❌ wudong-server/      # 旧版本，已合并到server/
❌ wudong-web/         # 旧版本，已合并到web/
❌ web-pc/             # 重复目录
❌ cool-admin-midway/  # 未使用的Cool Admin
❌ cool-admin-vue/     # 未使用的Cool Admin
❌ food-module/        # 已整合到server/src/module/food/
❌ 住模块/             # 已整合到server/src/module/hotel/
```

---

## 🔧 二、修复步骤

### Step 1: 统一数据库环境

#### 1.1 确认数据库配置

**标准配置（所有组员统一使用）：**

```env
DB_HOST=127.0.0.1
DB_PORT=3306          # 本地MySQL端口（非Docker则用3306）
DB_USER=root
DB_PASSWORD=你的密码  # 每个组员自己的本地密码
DB_NAME=wudong
```

#### 1.2 初始化数据库

```bash
# 1. 创建数据库
mysql -u root -p

# 在MySQL命令行中执行
DROP DATABASE IF EXISTS wudong;
CREATE DATABASE wudong CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
EXIT;

# 2. 导入DDL（表结构）
mysql -u root -p wudong < sql/01-ddl.sql

# 3. 导入DML（初始数据）
mysql -u root -p wudong < sql/02-dml.sql

# 4. 验证导入成功
mysql -u root -p wudong -e "SHOW TABLES;"
```

**预期结果：**
应该看到40+个表，包括：
- t_user, t_merchant, t_merchant_apply
- t_order, t_order_item
- prd_product, prd_category
- food_restaurant, food_dish
- sty_hotel, sty_room
- tvl_scenic, tvl_route
- note_post, note_topic
- ...

---

### Step 2: 完善缺失的接口

#### 2.1 为food模块添加dashboard接口

**文件：** `server/src/module/food/food.admin.ts`

在 `FoodAdminService` 类中添加（在stats方法后）：

```typescript
// ---------- 商家仪表盘 ----------
async dashboard(merchantId: number) {
  const dayjs = require('dayjs');

  // 1. 商品总数（菜品+农产品）
  const restaurant = await this.restaurantRepo.findOneBy({ merchantId });
  const dishCount = restaurant ? await this.dishRepo.countBy({ restaurantId: restaurant.id }) : 0;
  const farmCount = await this.farmRepo.countBy({ merchantId });
  const productCount = dishCount + farmCount;
  const onlineDishes = restaurant ? await this.dishRepo.countBy({ restaurantId: restaurant.id, status: 1 }) : 0;
  const onlineFarms = await this.farmRepo.countBy({ merchantId, status: 1 });
  const onlineProducts = onlineDishes + onlineFarms;

  // 2. 订单总量和今日订单
  const orderCount = await this.orderRepo.countBy({ merchantId });
  const today = dayjs().format('YYYY-MM-DD');
  const todayOrders = await this.orderRepo
    .createQueryBuilder('o')
    .where('o.merchant_id = :mid AND DATE(o.created_at) = :today', { mid: merchantId, today })
    .getCount();

  // 3. 销售总额和今日收入
  const revenueResult = await this.orderRepo
    .createQueryBuilder('o')
    .select('COALESCE(SUM(o.pay_amount), 0)', 'total')
    .where('o.merchant_id = :mid AND o.status >= 1 AND o.status NOT IN (5,7)', { mid: merchantId })
    .getRawOne();
  const totalRevenue = parseFloat(revenueResult?.total || '0');

  const todayRevenueResult = await this.orderRepo
    .createQueryBuilder('o')
    .select('COALESCE(SUM(o.pay_amount), 0)', 'total')
    .where('o.merchant_id = :mid AND o.status >= 1 AND o.status NOT IN (5,7) AND DATE(o.created_at) = :today', {
      mid: merchantId, today
    })
    .getRawOne();
  const todayRevenue = parseFloat(todayRevenueResult?.total || '0');

  // 4. 评价总数和待回复
  let reviewCount = 0;
  let pendingReviews = 0;
  if (restaurant) {
    reviewCount = await this.reviewRepo
      .createQueryBuilder('r')
      .where('r.biz_type = :type AND r.biz_id = :id', { type: 'restaurant', id: restaurant.id })
      .getCount();
    pendingReviews = await this.reviewRepo
      .createQueryBuilder('r')
      .where('r.biz_type = :type AND r.biz_id = :id AND (r.merchant_reply IS NULL OR r.merchant_reply = "")', {
        type: 'restaurant', id: restaurant.id
      })
      .getCount();
  }

  // 5. 销售趋势（近7天）
  const revenueTrend = [];
  for (let i = 6; i >= 0; i--) {
    const date = dayjs().subtract(i, 'day').format('YYYY-MM-DD');
    const dayRevenue = await this.orderRepo
      .createQueryBuilder('o')
      .select('COALESCE(SUM(o.pay_amount), 0)', 'revenue')
      .where('o.merchant_id = :mid AND o.status >= 1 AND o.status NOT IN (5,7) AND DATE(o.created_at) = :date', {
        mid: merchantId, date
      })
      .getRawOne();
    revenueTrend.push({
      date: dayjs(date).format('MM-DD'),
      revenue: parseFloat(dayRevenue?.revenue || '0')
    });
  }

  // 6. 订单状态分布
  const orderStatus = {
    pending: await this.orderRepo.countBy({ merchantId, status: 0 }),
    paid: await this.orderRepo.countBy({ merchantId, status: 1 }),
    shipped: await this.orderRepo.countBy({ merchantId, status: 2 }),
    completed: await this.orderRepo.countBy({ merchantId, status: 3 }),
    cancelled: await this.orderRepo.countBy({ merchantId, status: 5 })
  };

  return {
    productCount,
    onlineProducts,
    orderCount,
    todayOrders,
    totalRevenue,
    todayRevenue,
    reviewCount,
    pendingReviews,
    revenueTrend,
    orderStatus
  };
}
```

在 `FoodAdminController` 类中添加（在stats接口后）：

```typescript
@ApiOperation({ summary: '商家仪表盘数据' })
@Auth('merchant')
@Get('/dashboard')
async dashboard(@CurrentUserParam() user: CurrentUser) {
  return this.foodAdminService.dashboard(user.merchantId);
}
```

#### 2.2 为hotel模块添加dashboard接口

**文件：** `server/src/module/hotel/hotel.admin.ts`

类似地添加dashboard方法和接口（逻辑与food类似，统计酒店房间、订单、收入等）。

---

### Step 3: 统一配置文件

#### 3.1 创建标准.env.example模板

**文件：** `server/.env.example`

```env
# 应用配置
APP_KEYS=wudong-secret-key-2026
SERVER_PORT=7001
NODE_ENV=development

# 数据库配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=wudong

# Redis配置
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT配置
JWT_SECRET=wudong-jwt-secret-2026
JWT_EXPIRES_IN=7d

# 通义千问API（AI模块）
QIANWEN_API_KEY=your_api_key_here

# 支付配置（模拟）
PAY_MOCK_MODE=true

# 文件上传
UPLOAD_DIR=public/upload
```

#### 3.2 确保admin和web的配置正确

**admin/vite.config.ts:**
```typescript
server: {
  port: 5174,
  proxy: {
    '/api': {
      target: 'http://127.0.0.1:7001',
      changeOrigin: true
    }
  }
}
```

**web/vite.config.ts:**
```typescript
server: {
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://127.0.0.1:7001',
      changeOrigin: true
    },
    '/app': {
      target: 'http://127.0.0.1:7001',
      changeOrigin: true
    },
    '/upload': {
      target: 'http://127.0.0.1:7001',
      changeOrigin: true
    }
  }
}
```

---

### Step 4: 统一依赖安装

#### 4.1 后端依赖

```bash
cd server
rm -rf node_modules package-lock.json
npm install
```

**验证关键依赖：**
- @midwayjs/core
- @midwayjs/typeorm
- mysql2
- typeorm
- bcryptjs
- jsonwebtoken

#### 4.2 管理后台依赖

```bash
cd admin
rm -rf node_modules package-lock.json
npm install
```

#### 4.3 PC端依赖

```bash
cd qianduan0/web   # ⚠️ C 端在 qianduan0/web，根目录 web/ 已废弃
rm -rf node_modules package-lock.json
npm install
```

---

### Step 5: 验证启动

#### 5.1 启动后端

```bash
cd server
npm run dev
```

**成功标志：**
```
✅ 乌东文旅平台后端启动完成
✅ 服务运行在 http://127.0.0.1:7001
✅ Swagger文档: http://127.0.0.1:7001/swagger-ui
✅ 数据库连接成功
```

**测试接口：**
```bash
# 测试首页接口
curl http://localhost:7001/api/home

# 测试登录接口
curl -X POST http://localhost:7001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800000000","password":"admin123"}'
```

#### 5.2 启动管理后台

```bash
cd admin
npm run dev
```

**访问：** http://localhost:5174

**测试登录：**
- 账号：13800000000
- 密码：admin123

#### 5.3 启动PC用户端

```bash
cd qianduan0/web   # ⚠️ C 端在 qianduan0/web，根目录 web/ 已废弃
npm run dev
```

**访问：** http://localhost:5173

---

## 🧪 三、完整测试清单

### 3.1 后端模块测试

#### ✅ 衣模块（clothing）
- [ ] 商品列表：`GET /api/merchant/clothing/products`
- [ ] 商家仪表盘：`GET /api/merchant/clothing/dashboard`
- [ ] 订单管理：`GET /api/merchant/clothing/orders`

#### ✅ 食模块（food）
- [ ] 餐厅信息：`GET /api/merchant/food/restaurant`
- [ ] 商家仪表盘：`GET /api/merchant/food/dashboard` ⚠️ 需添加
- [ ] 预订管理：`GET /api/merchant/food/bookings`

#### ✅ 住模块（hotel）
- [ ] 酒店信息：`GET /api/merchant/hotel/info`
- [ ] 商家仪表盘：`GET /api/merchant/hotel/dashboard` ⚠️ 需添加
- [ ] 房间管理：`GET /api/merchant/hotel/rooms`

#### ✅ 行模块（travel）
- [ ] 景区列表：`GET /api/travel/scenics`
- [ ] 商家仪表盘：`GET /api/merchant/travel/dashboard` ✅ 已完成
- [ ] 路线管理：`GET /api/merchant/travel/routes`

#### ✅ 社区模块（community）
- [ ] 游记列表：`POST /app/note/feed`
- [ ] 发布游记：`POST /app/note/create`
- [ ] 话题列表：`POST /app/topic/list`

#### ✅ AI模块（ai）
- [ ] 聊天接口：`POST /api/ai/chat`

### 3.2 前端功能测试

#### 管理后台（admin）
- [ ] 登录功能
- [ ] 仪表盘显示（管理员/商家）
- [ ] 用户管理
- [ ] 商家审核
- [ ] 门票管理
- [ ] 社区管理

#### PC用户端（web）
- [ ] 首页显示
- [ ] 衣模块浏览
- [ ] 食模块预订
- [ ] 住模块预订
- [ ] 行模块（景区/路线）
- [ ] 社区功能
- [ ] AI聊天

---

## 📝 四、团队协作规范

### 4.1 Git工作流

```bash
# 1. 拉取最新代码
git pull origin main

# 2. 创建功能分支
git checkout -b feature/模块名-功能名

# 3. 开发并提交
git add .
git commit -m "feat: 功能描述"

# 4. 推送并创建PR
git push origin feature/模块名-功能名
```

### 4.2 代码提交规范

```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试
chore: 构建/工具变更
```

### 4.3 分支命名规范

```
main         # 主分支（稳定版本）
dev          # 开发分支
feature/*    # 功能分支
fix/*        # 修复分支
hotfix/*     # 紧急修复
```

---

## 🚨 五、常见问题解决

### Q1: 数据库连接失败
```
解决：
1. 检查MySQL是否启动
2. 确认端口号（3306或3307）
3. 验证密码是否正确
4. 检查.env配置
```

### Q2: 模块接口404
```
解决：
1. 确认模块是否在configuration.ts中注册
2. 检查路由路径是否正确
3. 查看后端日志
```

### Q3: 前端页面空白
```
解决：
1. 检查后端是否启动
2. 打开F12查看console错误
3. 检查Network请求状态
4. 确认代理配置
```

### Q4: 跨域问题
```
解决：
前端已配置proxy，确保：
1. 后端在7001端口
2. vite.config中proxy配置正确
3. 重启前端服务
```

---

## ✅ 六、验收标准

### 最低验收标准（MVP）

- [x] 数据库初始化成功，40+张表
- [ ] 后端启动无错误，7001端口监听
- [ ] 管理后台启动，5174端口可访问
- [ ] PC端启动，5173端口可访问
- [ ] 管理员可登录后台
- [ ] 用户可访问PC端首页
- [ ] 四大模块（衣食住行）后端API可调用
- [ ] 社区功能正常（发布、浏览）

### 完整验收标准

在MVP基础上：
- [ ] 所有模块dashboard接口完整
- [ ] 前端四大模块页面完整
- [ ] 订单流程完整（下单、支付、核销）
- [ ] AI聊天功能正常
- [ ] 商家可登录管理后台
- [ ] 数据统计图表显示正常

---

## 📅 七、整合时间规划

### Day 1: 环境统一
- 统一数据库配置
- 导入SQL脚本
- 安装所有依赖

### Day 2: 接口补全
- 完成food模块dashboard
- 完成hotel模块dashboard
- 测试所有后端接口

### Day 3: 前端集成
- 确认前端路由完整
- 测试前后端对接
- 修复API调用问题

### Day 4: 联调测试
- 完整业务流程测试
- 修复发现的bug
- 优化用户体验

### Day 5: 文档整理
- 更新部署文档
- 编写使用手册
- 准备演示数据

---

## 📞 八、联系方式

如遇问题，请在项目群中@相关负责人：

- **项目总负责人**: xxx
- **衣模块**: xxx
- **食模块**: xxx
- **住模块**: xxx
- **行模块**: xxx
- **社区模块**: xxx
- **前端负责人**: xxx
- **数据库负责人**: xxx

---

**文档版本**: v1.0  
**创建时间**: 2026-09-11  
**最后更新**: 2026-09-11
