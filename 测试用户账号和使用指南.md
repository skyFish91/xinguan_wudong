# 测试用户账号和使用指南

## 📝 测试账号信息

### 主测试账号
- **手机号**: 13800138000
- **密码**: 123456
- **昵称**: 测试用户
- **角色**: USER

### 其他测试账号
| 手机号 | 密码 | 昵称 | 用途 |
|--------|------|------|------|
| 13800138001 | 123456 | 旅行达人小王 | 测试关注、互动 |
| 13800138002 | 123456 | 摄影师张三 | 测试评论、点赞 |
| 13800138003 | 123456 | 美食博主李四 | 测试多用户场景 |

## 🚀 系统访问地址

### 前端PC端
- **地址**: http://localhost:3001/
- **社区首页**: http://localhost:3001/community
- **状态**: ✅ 运行中

### 后端API
- **地址**: http://localhost:8001/
- **Swagger文档**: http://localhost:8001/swagger-ui/index.html
- **状态**: ✅ 运行中

## 🔐 登录测试

### 方法1：密码登录
**接口**: `POST http://localhost:8001/app/user/login/password`

**请求体**:
```json
{
  "phone": "13800138000",
  "password": "123456"
}
```

**响应**:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "expire": 604800,
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refreshExpire": 2592000,
    "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

### 方法2：前端页面登录（需要实现登录页面）

目前前端可能还没有登录页面，可以通过以下方式测试：

#### 使用浏览器控制台直接设置token：
1. 打开浏览器开发者工具（F12）
2. 切换到Console标签
3. 执行以下代码获取token：

```javascript
fetch('http://localhost:8001/app/user/login/password', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    phone: '13800138000',
    password: '123456'
  })
})
.then(res => res.json())
.then(data => {
  if (data.code === 0) {
    // 保存token到localStorage
    localStorage.setItem('token', data.data.token)
    console.log('登录成功，token已保存:', data.data.token)
    // 刷新页面
    location.reload()
  } else {
    console.error('登录失败:', data.message)
  }
})
```

## 📤 发布游记测试流程

### 步骤1：确保已登录
在浏览器控制台执行：
```javascript
console.log('Token:', localStorage.getItem('token'))
// 应该显示一个JWT token，如果是null则需要先登录
```

### 步骤2：访问社区首页
http://localhost:3001/community

### 步骤3：点击"发布游记"按钮

### 步骤4：上传图片
- 点击图片上传区域
- 选择图片文件（≤10MB）
- 或者上传视频（≤100MB，与图片二选一）

### 步骤5：填写内容
- **标题**: 2-50个字符
- **正文**: 10-5000个字符
- **话题**: 可选，多选
- **关联地点**: 可选

### 步骤6：点击"发布"

### 预期结果
- ✅ 上传成功，图片显示预览
- ✅ 表单验证通过
- ✅ 发布成功，提示"发布成功，等待审核"
- ✅ 对话框关闭

## 🔧 如果上传失败的排查步骤

### 检查1：是否已登录
```javascript
// 在浏览器控制台执行
console.log('Token:', localStorage.getItem('token'))
```
如果没有token，先执行登录代码

### 检查2：后端服务是否正常
访问: http://localhost:8001/
应该返回一些响应，而不是无法连接

### 检查3：检查网络请求
1. 打开浏览器开发者工具（F12）
2. 切换到 Network 标签
3. 上传图片
4. 查看是否有请求到 `/app/comm/upload`
5. 查看请求的响应状态和内容

### 检查4：查看控制台错误
打开浏览器控制台（F12 → Console），查看是否有错误信息

## 🐛 常见问题解决

### 问题1：401 Unauthorized 未授权
**原因**: 未登录或token过期
**解决**: 重新执行登录代码

### 问题2：上传接口返回404
**原因**: 后端服务未启动或接口路径错误
**解决**: 
- 检查后端是否运行在 http://localhost:8001/
- 确认已修复API路径为 `/app/comm/upload`

### 问题3：图片预览不显示
**原因**: 返回的URL格式不正确或无法访问
**解决**: 
- 检查返回的URL格式
- 访问 http://localhost:8001/upload/ 测试静态资源

### 问题4：CORS跨域错误
**原因**: 前后端端口不同，需要配置代理
**解决**: 检查 `web-pc/vite.config.js` 的 proxy 配置

## 📋 完整测试清单

### 用户功能
- [ ] 密码登录成功
- [ ] Token保存到localStorage
- [ ] 刷新页面后登录状态保持

### 上传功能
- [ ] 单张图片上传成功
- [ ] 多张图片上传（≤9张）
- [ ] 视频上传成功
- [ ] 上传进度显示
- [ ] 图片预览正常
- [ ] 视频预览正常

### 发布游记
- [ ] 标题验证（2-50字符）
- [ ] 正文验证（10-5000字符）
- [ ] 必须上传图片或视频
- [ ] 话题选择正常
- [ ] 地点选择正常
- [ ] 发布成功

### 社区浏览
- [ ] 首页列表显示
- [ ] 瀑布流布局正常
- [ ] 点击卡片进入详情
- [ ] 详情页显示完整

## 🎯 下一步开发建议

### 优先级P0（必须）
1. **创建登录页面** (`web-pc/src/views/auth/Login.vue`)
   - 手机号+密码登录
   - 表单验证
   - 登录成功后跳转

2. **完善token管理**
   - 自动刷新token
   - token过期自动跳转登录
   - 退出登录清除token

3. **完善上传功能**
   - 更好的错误提示
   - 上传前图片压缩
   - 重试机制

### 优先级P1（重要）
1. **用户中心页面**
   - 个人信息展示
   - 修改昵称、头像
   - 退出登录

2. **完善发布功能**
   - 草稿保存
   - 图片编辑
   - 视频封面选择

3. **社区互动**
   - 点赞功能
   - 评论功能
   - 收藏功能

## 💡 快速登录脚本（方便测试）

将以下代码保存为书签，点击即可快速登录：

```javascript
javascript:(function(){fetch('http://localhost:8001/app/user/login/password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone:'13800138000',password:'123456'})}).then(r=>r.json()).then(d=>{if(d.code===0){localStorage.setItem('token',d.data.token);alert('登录成功！');location.reload()}else{alert('登录失败：'+d.message)}})})()
```

## 📞 技术支持

如果遇到问题，请检查：
1. 前端服务: http://localhost:3001/
2. 后端服务: http://localhost:8001/
3. 数据库连接: MySQL 3308端口
4. 查看后端日志
5. 查看浏览器控制台错误

## 🎉 测试成功标准

完成以下流程代表系统基本可用：
1. ✅ 使用测试账号登录成功
2. ✅ 访问社区首页看到界面
3. ✅ 点击发布游记打开对话框
4. ✅ 上传图片成功并显示预览
5. ✅ 填写标题和正文
6. ✅ 点击发布成功

祝测试顺利！🚀
